services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: autolog
      POSTGRES_PASSWORD: autolog_dev
      POSTGRES_DB: autolog
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: